#include <raylib.h>
#include "ball.h"

int main()
{
    InitWindow(300, 600, "Tetris Game");
    SetTargetFPS(60);

    while (!WindowShouldClose())
    {
       

        BeginDrawing();
        
        
        EndDrawing();
    }

    CloseWindow();
    return 0;
}